# RTC 1.25 / 1.5 / 1.75 固定输入诊断

2026-09-19。入口 `scripts/probe_rtc_strength_above1.py`，输出 `artifacts/rtc_strength_above1_probe_v1/summary.json`。不运行新闭环、不改模型、不覆盖历史结果。

复用 `rtc_guidance_diagnosis_v1` 的12个观测、各2份显式噪声及同一旧动作前缀，预计延迟3/5 tick，每强度48例。重新计算0和1作为参考，共240次预测。S1权重和输入文件哈希检查通过，参考输出在预设rtol=1e-5、atol=1e-6内重现；每次一次t=1前向，输出有限，参数前后哈希不变。脚本编译通过，现有RTC单元测试15项通过。

| 强度 | 原整块门槛通过 | 预计执行区间误差/无引导误差 | 执行区间越过旧动作目标的案例 |
|---|---:|---:|---:|
| 0 | 48/48 | 1.0000 | 0/48 |
| 1 | 48/48 | 0.2772 | 0/48 |
| 1.25 | 30/48 | 0.1906 | 0/48 |
| 1.5 | 22/48 | 0.1416 | 24/48 |
| 1.75 | 19/48 | 0.1302 | 48/48 |
| 2 | 19/48 | 0.1564 | 48/48 |

用户随后要求补测2。使用相同入口新增 `--weights 2`，独立输出 `rtc_strength2_probe_v1`，重新运行0/1参考和2共144次预测。参考复现与参数不变检查均通过。2的执行区间最大单坐标外推幅度为1.293758（归一化单位），1.5为0.208742，1.75为0.751251。2相对无引导的整块前缀误差比为0.819857，执行区间误差比为0.156399。误差比是各案例误差总和之比，不是闭环成功率。新增2同样未全部通过原整块门槛，没有运行闭环。

原门槛保持为整块加权前缀误差不增加（余量1e-6）、整块最大绝对归一化动作不超过无引导/旧前缀全局幅值包络（余量1e-5）。所有新强度前一项通过，失败来自幅值项。没有重新定义门槛使候选通过。

新增执行区间诊断取 `[estimated_delay:10]`，按原EXP权重计算旧前缀接近程度。越界指单坐标超出无引导动作和旧动作目标之间的区间，阈值1e-5；它不是环境物理安全界。预计延迟3/5不等同于未来闭环每次的实际过期长度。1.25整块超包络只涉及此诊断中将被丢弃的前缀；这一点不构成全面闭环保证。

S1单步中引导满足 a_guided=a_plain+g*w*(a_old-a_plain)。延迟3时首个可用位置w约0.71235，三个候选系数约0.8904、1.0685、1.2466；延迟5时w约0.63095，系数约0.7887、0.9464、1.1042。超过1意味着越过旧目标，仍可能降低总体前缀MSE。

接近旧动作更多不代表任务成功率更高，也不能证明RTC比朴素异步好。1.25可作为优先讨论的闭环候选；1.5/1.75有明确外推。三个候选均未全部通过原门槛，本轮止于诊断。若继续闭环，需要明确批准其探索性风险及协议变更，不再使用已看过的成功率作为未触及测试证明。

执行命令（远端既有运行环境）：

```sh
PYTHONPATH=src:. HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 MUJOCO_GL=egl \
/root/autodl-tmp/venvs/smolvla-flow/bin/python scripts/probe_rtc_strength_above1.py \
  --source /root/autodl-tmp/outputs/rtc_guidance_diagnosis_v1 \
  --output /root/autodl-tmp/outputs/rtc_strength_above1_probe_v1 \
  --checkpoint /root/autodl-tmp/checkpoints/smolvla_libero \
  --student /root/autodl-tmp/outputs/distill40_s1_formal_v1/training/010000.pt
```
