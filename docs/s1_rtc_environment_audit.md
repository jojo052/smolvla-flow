# S1 RTC 环境步审计（2026-09-19）

本轮仅诊断与重新预检，不覆盖或追加旧的 204 条正式回合。控制频率 20 Hz、迟到超过 5 ms 的 tick 占比不超过 5%，以及模型、任务、动作协议均不改动。

## 固定动作重放

入口：`scripts/probe_rtc_env_step.py`。原始数据来自 split-process 正式运行中 Spatial task 5 / init 21 / async +100 ms 的失败记录，包含 94 tick。原运行迟到率为 13.83%。

逐 tick 重放保存动作，并在原请求 tick 运行后台模型预测以保留计算竞争；预测输出不替换保存动作。本检查不生成新的成功率，不等于完整闭环重跑。

结果保存于 `artifacts/rtc_env_replay_v1/`。时间单位为 ms。

| 配置 | 重复 | env.step 平均 | P95 | 迟到 tick 占比 |
|---|---:|---:|---:|---:|
| 原配置 | 1 | 34.22 | 43.15 | 0% |
| 原配置 | 2 | 34.04 | 42.28 | 0% |
| 原配置 | 3 | 34.09 | 43.04 | 0% |
| 环境/推理 CPU 分离绑定 | 1 | 35.80 | 45.82 | 2.13% |
| 环境/推理 CPU 分离绑定 | 2 | 36.39 | 46.60 | 3.19% |
| 环境/推理 CPU 分离绑定 | 3 | 34.55 | 44.13 | 0% |

容器 CPU quota 为 20 核时间额度，允许 CPU 集为 0–207。绑定测试给环境 2 个、模型 4 个不同物理核心上的逻辑 CPU；这不意味着独占物理核心。绑定没有表现出收益，不用于后续预检。

六次重放全部观测数组的哈希相同：`615046c368dba87a6eac26e0582fffba25586cac478a33bed5c109a50fc8060c`。该结论只覆盖本次重放之间，未证明与历史失败回合的实际观测逐值相同。

原配置三次最大环境步均约 62.5 ms，发生在最后一个 tick（93）。迟到率测量的是动作步开始相对截止时间的偏差，最后一步没有后续动作边界，因此其超时未增加本片段的迟到 tick 数。0% 不代表每次环境计算都低于 50 ms。重放也未覆盖正式循环的全部调度、视频帧复制和资源检查开销。

## 热点与改动决策

原配置中，物理 forward 与机器人控制的包含式计时各约 14.2 ms，物理 step 约 7.7 ms；相机渲染与图像读取合计约 1.3 ms，观测组装约 0.13 ms。计时存在嵌套，不能将各项直接相加。

当前主要热点位于 CPU 物理与控制路径。未发现值得通过图像复制优化解决的主要瓶颈。不删除控制器内的 forward、不改求解器、仿真步长、渲染分辨率或频率。当前没有采用任何缺乏语义一致性证据的热点改动。

原配置三次均未重现连续迟到，说明故障并非在这段固定动作上稳定发生；不能据此证明原故障已经修复，或断言其唯一原因是外部资源争抢。需要保留原门控并做完整预检。

六次结果已完整保存，诊断进程退出码为 0。退出清理时出现 EGL_NOT_INITIALIZED 析构警告；不将其隐去，也不将其当作已完成回合的计时失败。

## 独立重新预检

使用原 split-process 配置，输出到 `/root/autodl-tmp/outputs/rtc_env_revalidation_v1`，仅运行 `scripts.evaluate_s1_rtc --split-process --stage check`。

四套件 task 0 / init 13，三方式乘两延迟，共 24 回合。最终状态以该目录的 `gate.json` 和逐回合记录为准，不以固定动作重放通过替代闭环验收。没有配置预检通过后自动运行正式评测。

预检已完成：`gate.json` 为 `passed: true`，24 条完整记录、24 个视频、7,035 个控制 tick，超过 5 ms 的迟到 tick 为 0；每回合迟到率均为 0%。实际已接纳请求的最大延迟为 5 tick。逐回合端到端耗时合计 483.25 秒，不含进程启动等回合外开销。

运行标识为 `177aca1890e0beaca32e2719a9731ad85a8d5240725e413ceffa75b2a5def591`，与原 split-process 配置一致。全部预检输出已备份到 `artifacts/rtc_env_revalidation_v1/`。本地相关单元测试命令 `PYTHONPATH=src:. python3 -m unittest discover -s tests -p 'test_rtc*.py' -q`，15 项通过。

结论：本次预检通过，但没有证据表明环境长尾已被永久消除。CPU 绑定候选被拒绝，没有更改正式执行器。旧正式结果仍为 204 条，尚未续跑剩余 36 回合。

## 用户授权恢复

随后用户明确确认“按原配置”。2026-09-19 启动 screen `rtc-original-resume`，恢复原输出目录 `s1_rtc_split_formal_v1`，使用 `--split-process --stage resume --acknowledge-errors`。后者仅归档已经审计的错误标记，不删除失败证据。恢复入口验证并跳过已有完整回合，没有自动重试循环。

日志：`/root/autodl-tmp/logs/rtc_original_resume_20260919.log`。配置仍为 `configs/s1_rtc_realtime_v1.json`，S1 `010000.pt`，原 baseline manifest 和原官方权重；未修改 CPU 绑定、20 Hz、5 ms / 5% 门控、RTC horizon 或动作协议。再次发生执行错误时停止，不自动开关机。
